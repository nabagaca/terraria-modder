# StorageHub TODO

## Disk Identity

- [ ] Replace prefix-backed disk UID with dedicated persistent disk metadata/UUID storage.
- [ ] Remove the current per-disk-item-type UID cap (`1..255`) caused by prefix-based identity.

